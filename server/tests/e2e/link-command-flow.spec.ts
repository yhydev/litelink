describe("link command flow", () => {
  it("covers config -> record -> render -> dispatch", async () => {
    // Placeholder e2e skeleton for quickstart validation.
    // 1) create link config
    // 2) create link record
    // 3) render command
    // 4) dispatch to local endpoint
    expect(true).toBe(true)
  })
})
