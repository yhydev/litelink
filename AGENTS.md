<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read `specs/001-configure-link-commands/plan.md`
<!-- SPECKIT END -->

## 协作约定（中文）

- 对话语言统一使用中文；代码、命令、路径、接口名保持原文。
- 回答优先给出结论，再给关键细节；避免冗长背景描述。
- 在修改代码前，先快速说明将改哪些文件与目的。
- 如需求存在歧义，先基于仓库现状做最合理默认，再明确写出默认假设。
- 完成改动后，提供：变更文件列表、核心改动点、验证方式（如测试/构建命令）。
- 非用户明确要求，不做破坏性操作（如删除大量文件、重置历史、强推）。

## 代码变更原则

- 优先小步提交，单次改动聚焦一个目标，避免“顺手重构”。
- 严格遵循现有目录结构、命名风格与 lint/format 规则。
- 先修复根因，避免仅通过绕过校验或降级约束“表面修复”。
- 涉及安全能力（命令执行、鉴权、输入校验）时，默认采用最小权限与白名单策略。
- 新增配置项时补充默认值、边界校验与必要文档说明。

## 验证与交付

- 能运行测试则优先运行与改动相关的最小测试集；失败时说明原因与下一步。
- 若无法在当前环境完成验证，明确列出用户可直接执行的验证命令。
- 输出中使用简洁清单，便于快速审阅与复制执行。
